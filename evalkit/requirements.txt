# Always needed
numpy>=1.24
opencv-python>=4.8
pycocotools>=2.0.7
matplotlib>=3.7
pyyaml>=6.0
tqdm>=4.65

# Per-format, install only what you use
# ultralytics>=8.3        # --format ultralytics
# onnxruntime-gpu>=1.17   # --format onnx
# torch>=2.0              # --format torchscript / visionkit / .pt auto-detect
